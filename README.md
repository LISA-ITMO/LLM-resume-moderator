# LLM Resume Moderator

API-сервис для автоматической модерации резюме с использованием больших языковых моделей (LLM). Сервис проверяет резюме на соответствие правилам, валидирует документы об образовании с помощью OCR и нормализует специальности.

## Описание

LLM Resume Moderator — это FastAPI-приложение, предназначенное для автоматизации процесса отбора кандидатов в студенческий резерв. Сервис выполняет следующие функции:

- **Модерация резюме**: Проверка резюме на соответствие заданным правилам с использованием LLM
- **Валидация документов об образовании**: Распознавание и проверка PDF-документов (дипломов, справок) с помощью OCR
- **Нормализация специальностей**: Извлечение и стандартизация информации о специальностях из резюме и документов
- **Проверка соответствия**: Сопоставление специальностей с перечнем требуемых специальностей

## Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Python 3.12 (для локальной разработки)

Команды для запуска (Docker compose):
```bash
git clone https://github.com/LISA-ITMO/LLM-resume-moderator.git &&
cd LLM-resume-moderator &&
docker-compose up &&
```
Сервис будет доступен по адресу `http://localhost:8000`

## Основные возможности

- ✅ Проверка резюме на соответствие правилам (язык, токсичность, формат)
- ✅ OCR-распознавание документов об образовании (PaddleOCR)
- ✅ Нормализация специальностей с использованием LLM и regex
- ✅ Валидация соответствия специальностей перечню
- ✅ Поддержка локальных и облачных LLM-провайдеров
- ✅ RESTful API с автоматической документацией (Swagger/OpenAPI)

## Технологический стек

- **Backend**: FastAPI, Python 3.12
- **LLM**: Nexa SDK (поддержка локальных и облачных моделей)
- **OCR**: PaddleOCR для распознавания текста из PDF
- **Контейнеризация**: Docker, Docker Compose
- **Зависимости**: См. `requirements.txt`


## Конфигурация

### Правила модерации

Правила модерации резюме настраиваются в файле `configs/resume_rules.json`:

```json
[
  {
    "id": "rule_4",
    "condition": "Резюме должно быть заполнено на русском языке..."
  },
  {
    "id": "rule_7",
    "condition": "Информация не должна содержать слова и выражения..."
  }
]
```

### Требуемые специальности

Список требуемых специальностей настраивается в `configs/required_specialties.py`.

### LLM провайдеры

Настройки LLM-провайдеров находятся в `configs/config.py`. Поддерживаются:
- `local`: Локальные модели через Nexa SDK
- `caila.io`: Облачный провайдер

## Структура проекта

```
LLM-resume-moderator/
├── configs/              # Конфигурационные файлы
│   ├── config.py        # Основные настройки
│   ├── logging_config.py # Настройки логирования
│   ├── resume_rules.json # Правила модерации резюме
│   ├── required_specialties.py # Перечень требуемых специальностей
│   └── slovar.py        # Словарь специальностей
├── service/              # Бизнес-логика
│   ├── rules_moderator.py      # Модерация по правилам
│   ├── education_normalizer.py # Нормализация специальностей
│   ├── OCR.py                  # OCR-сервис
│   ├── llm_interface.py        # Интерфейс для работы с LLM
│   ├── resume_text_converter.py # Конвертация резюме в текст
│   └── utils.py                # Утилиты
├── data/                 # Данные и модели
│   ├── docs/            # Документация
│   ├── nexa_sdk/        # Модели LLM
│   └── resumes/         # Примеры резюме
├── storage/              # Временное хранилище загруженных файлов
├── tests/                # Тесты
├── notebooks/            # Jupyter notebooks для анализа
├── main.py              # Точка входа приложения
├── routers.py           # API-роутеры
├── schemas.py           # Pydantic-схемы
├── docker-compose.yaml  # Docker Compose конфигурация
├── Dockerfile           # Docker образ
└── requirements.txt     # Зависимости Python

```

## Особенности реализации

### Модерация резюме

Модерация выполняется с помощью LLM, которая анализирует текст резюме на соответствие правилам. Поддерживаются модели с reasoning (рассуждениями) и без.

### OCR-распознавание

Используется PaddleOCR с оптимизированными моделями для кириллицы:
- Детекция текста: `PP-OCRv5_mobile_det`
- Распознавание: `cyrillic_PP-OCRv5_mobile_rec`
- Классификация ориентации и выпрямление документа

### Нормализация специальностей

Процесс нормализации включает:
1. Regex-парсинг для извлечения кода специальности
2. LLM-нормализация при отсутствии кода
3. Поиск в справочнике специальностей по коду или названию

## Контакты

Для вопросов и предложений создайте issue в репозитории.


## **Доступ к демо**

Сервис уже развёрнут и доступен по адресам:

- api url: http://89.169.149.254:8000

- Swagger-документация: http://89.169.149.254:8000/docs

  

## **Публикации и доклады:**

1. **Шилоносов В.Р.** (науч. рук. Федоров Д.А.)

[**Сервис автоматической модерации резюме на русском языке**](https://kmu.itmo.ru/digests/article/15750) Сборник тезисов докладов конгресса молодых ученых. Электронное издание. – СПб: Университет ИТМО, 2025.

  

2. **Выступление с докладом "Сервис автоматической модерации резюме на русском языке"**

XIV Конгресс молодых ученых ИТМО (Секция «Искусственный интеллект», подсекция «Интеллектуальные сервисы и приложения»)

📺 Видеозаписи:

[![YouTube](https://img.shields.io/badge/-YouTube-%23FF0000?style=flat&logo=youtube&logoColor=white)](https://youtu.be/gVLFGFRvW-o)

[![VK Video](https://img.shields.io/badge/-VK%20Video-%230077FF?style=flat&logo=vk&logoColor=white)](https://vkvideo.ru/video-223020743_456239042?t=3h44m23s)

  

## 📬 Контакты

Telegram: `@Vlodimirshil`

Email: `vladimir@itmo.ru`